// Only supported on Chromium
if ("PaymentRequest" in window) {
	// PaymentRequestEvent.topOrigin = $aero.proxyLocation.origin;
	// MerchantValidationEvent.validationURL = $aero.afterPrefix(MerchantValidationEvent.validationURL);
	// ...
}
