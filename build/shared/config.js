const namingSchemes = {
  entryName: "aero-[name]-[hash].[ext]",
  chunkName: "aero-chunk-[name]-[hash].[ext]",
};

const src = "../src/";
const outdir = "../dist/";

export { namingSchemes, src, outdir };
