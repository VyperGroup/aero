// TODO: This will generate a file `dist/biome/globals.biome.json` this will define globals for every feature flag in AeroSandbox. It will be included with `extends` in `src/AeroSandbox/biome.json`.
// TODO: There will also be `dist/biome/globalsSW.biome.json` which will define globals for every feature flag in SW. It will be included in `extends` in `biome.json`.
