export function initApis() {
	// TODO: Look for every API interceptor just like how init APIs does, but instead create a file called `apis.d.ts`, with as a single type exported in it. That type would replace `export enum APIBitwiseEnum {}` from `apiInterceptors.d.ts`
}