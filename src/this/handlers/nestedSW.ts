export default () => {
	// Not implemented yet
	// TODO: Send the function src of newly "installed SWs" (fake) through messages, eval the event handlers in the SW, and route the handlers
	// TODO: Check the URL to make sure that it is formatted properly
};
