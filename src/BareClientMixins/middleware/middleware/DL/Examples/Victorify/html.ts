/*
import { HTMLMiddleware } from "middleware";

// @ts-nocheck
import { getAssetUrl } from "$lib";

const lib: HTMLMiddleware = {
	handle: el => {
		if (el instanceof HTMLImageElement) el.src = getAssetUrl("/vicr.svg");
	},
};

export default lib;
*/
