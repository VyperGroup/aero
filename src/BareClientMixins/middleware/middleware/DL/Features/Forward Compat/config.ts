// These configuration options will also be available to the user via a settings menu (cookies) and url parameters
import { Config } from "configType";

const config: Config = {
	convertVideosToGifs: false,
	fontPrerendering: true,
};

export default config;
