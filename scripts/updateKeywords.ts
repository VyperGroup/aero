/**
 * @module
 * TODO: Make a script, which fetches the keywords from aero's GitHub repo and updates the keywords in the package.json accordingly. It will use the GitHub API to fetch the keywords from the repo.
 * TODO: Make a workflow to update the keywords whenever they are changed (check every week) that would use this script.
 */
