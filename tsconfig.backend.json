{
  "compilerOptions": {
    // This might need to be changed in the future:
    "moduleResolution": "nodenext",
    "baseUrl": "./src",
    "composite": true
  },
  "include": ["src/altBackends/**/*.ts", "types/*.d.ts"]
}
