{
  "compilerOptions": {
    // This might need to be changed in the future:
    "moduleResolution": "nodenext",
    "baseUrl": "./src",
    "composite": true
  },
  "include": ["src/backend/**/*.ts", "types/*.d.ts"]
}
