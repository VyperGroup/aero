/**
 * Sec interface.
 * This interface is used to define the security options.
 */
export interface Sec {
	clear: string[];
	timing: string;
	permsFrame: string;
	perms: string;
	frame: string;
	csp: string;
}
